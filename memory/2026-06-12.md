## 2026-06-12

- 修复 AtmosphereFX 的 OrbitControls 缩放上限，避免导入 FBX 后滚轮缩放过早卡住。
- 为粒子和烟雾增加可调原点偏移滑杆，并接到 UI 事件与运行时中心点逻辑。
- 烟雾纹理改为不规则多团块 + 边缘咬孔，减轻明显圆形粒子边框。
- 预览页版本已更新到 `index.html?v=8`。
- 修复退出摄像机视角后粒子看不见：粒子/烟雾特效锚点改用与 FBX 环绕相机预览一致的看向模型方向；自由视角适配范围包含模型、摄像机轨迹和特效当前位置。预览页版本更新到 `v=9`。
- 继续修复退出摄像机视角后粒子不可见：自由视角改为优先框住当前特效区域，不再强制适配整条摄像机轨迹；粒子材质关闭深度测试，避免被模型遮住。粒子所有滑杆范围扩大一倍，内部扰动/旋转/变化参数上限同步放开。预览页版本更新到 `v=10`。
- 修正“退出摄像机视角”交互：不再自动 fit / 改变角度 / 取消选择，只关闭主相机跟随并保持当前画面，让用户从同一视角自由移动继续看特效。预览页版本更新到 `v=11`。
- 明确粒子/烟雾 VFX 是场景元素而非相机附件：主循环移除粒子/烟雾跟随 FBX 摄像机逻辑，原点 X/Y/Z 作为场景生成中心。预览页版本更新到 `v=12`。
- 将 UI 中“扩散范围”改为“生成范围”，并修复烟雾范围滑杆拖动后不会立即重布的问题。预览页版本更新到 `v=13`。
